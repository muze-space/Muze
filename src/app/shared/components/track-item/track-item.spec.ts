describe('TrackItem', () => {
  it('is testable', () => {
    expect(1).toBe(1);
  });

  it('should render track item', () => {
    expect(true).toBeTruthy();
  });

  it('should display track name', () => {
    expect(true).toBeTruthy();
  });
});
